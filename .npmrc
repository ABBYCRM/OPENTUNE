# Render web build doesn't need serialport (it's a native module for desktop).
# We let everything else install normally, just skip the postinstall that
# tries to compile serialport.
ignore-scripts=true
